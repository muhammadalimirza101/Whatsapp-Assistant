{
  "name": "@wa/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Placeholder. The Vercel OAuth functions now live at the repo-root api/ directory (Vercel builds from root). See ../../api and ../../lib/oauth.ts.",
  "scripts": {
    "typecheck": "echo \"@wa/web: functions moved to repo-root api/ (typechecked via tsconfig.api.json)\" && exit 0"
  }
}
